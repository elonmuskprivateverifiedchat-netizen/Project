{
  "provider": "nodejs",
  "install": "pnpm install --frozen-lockfile",
  "build": "pnpm --filter @workspace/trading-platform run build",
  "start": "pnpm --filter @workspace/trading-platform run serve",
  "env": {
    "NODE_ENV": "production",
    "PORT": "3000"
  }
}
